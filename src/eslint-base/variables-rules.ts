import type { VariablesRules } from './types';

const rules: VariablesRules = {
    'no-delete-var': ['off'],
    'no-label-var': ['off'],
    'no-undef': ['off'],
    'no-undef-init': ['off'],
    'no-undefined': ['off']
};

export default rules;
